/*
function idDetailFormatter(index, row) {
	return index + '. Item ID: ' + row.id
}

function principal_tecDetailFormatter(index, row) {
	return index + '. Item Tec/Ativ Principal: ' + row.principal_tec
}

function outras_tecDetailFormatter(index, row) {
	return index + '. Item Outras Tecnologias: ' + row.outras_tec
}

function formacaoDetailFormatter(index, row) {
	return index + '. Item Formação Acadêmica: ' + row.formacao
}

function certificacaoDetailFormatter(index, row) {
	return index + '. Item Certificações: ' + row.certificacao
}

function experienciaDetailFormatter(index, row) {
	return index + '. Item Tempo de Experiência: ' + row.experiencia
}

function idiomasDetailFormatter(index, row) {
	return index + '. Item Idiomas: ' + row.idiomas
}

function restricoesDetailFormatter(index, row) {
	return index + '. Item Restrições Local: ' + row.restricoes
}

function possui_filhosDetailFormatter(index, row) {
	return index + '. Item Possui filhos: ' + row.possui_filhos
}

function qtd_filhosDetailFormatter(index, row) {
	return index + '. Item Quant filhos: ' + row.qtd_filhos
}

function idade_filhosDetailFormatter(index, row) {
	return index + '. Item Idade filhos: ' + row.idade_filhos
}

function priceDetailFormatter(index, row) {
	return index + '. Item Price: ' + row.price
}*/